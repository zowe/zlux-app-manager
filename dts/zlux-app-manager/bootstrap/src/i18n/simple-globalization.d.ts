export declare class SimpleGlobalization implements ZLUX.Globalization {
    getLanguage(): string;
    getLocale(): string;
    setLanguage(language: string): any;
    setLocale(locale: string): any;
}
