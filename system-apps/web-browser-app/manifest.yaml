---
name: web-browser
id: org.zowe.zlux.ng2desktop.webbrowser
# Without the v
version: 2.0.0
# Human readable component name
title: 'Web Browser'
# Human readable component description
description: 'Web Browser Application'
homepage: https://zowe.org
keywords:
  - zlux
  - appfw
  - app
  - angular
  - browser
license: EPL-2.0
repository:
  type: git
  url: https://github.com/zowe/zlux-app-manager.git
# we do not specify encoding here because its already tagged ascii
build:
  branch: "{{build.branch}}"
  number: "{{build.number}}"
  commitHash: "{{build.commitHash}}"
  timestamp: "{{build.timestamp}}"
appfwPlugins:
  - path: .
