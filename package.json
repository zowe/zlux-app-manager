{
  "name": "zlux-app-manager",
  "version": "0.0.0-zlux.version.replacement",
  "private": true,
  "description": "Unit tests for zlux-app-manager",
  "scripts": {
    "test": "mocha --require ./test/test-setup.js --require ts-node/register 'test/*.js'",
    "test:coverage": "c8 --reporter=lcov --reporter=text mocha --require ./test/test-setup.js --require ts-node/register 'test/*.js'"
  },
  "devDependencies": {
    "c8": "10.1.3",
    "chai": "4.3.10",
    "mocha": "10.3.0",
    "sinon": "17.0.1",
    "ts-node": "10.9.2",
    "typescript": "4.2.4"
  }
}
